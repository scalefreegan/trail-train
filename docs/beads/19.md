## What
Regression harness for the whole pipeline: (1) re-derive MM100 through intake from its cached `sources/` and diff against the archived folder; (2) intake the San Juan Softie 2027 from the live site + 2026 manual + Gaia GPX and check the assertions listed in PRD §12; (3) `grep` gate for race literals from the literal-removal bead; (4) `npm run build` and `npm test`. Wire as `npm run check:races` and document in README. Also update `docs/PRD-modular-races.md` status to "implemented" with a changelog of deviations.

## Why
Four phases of work across scripts and client; this is the exit test for the epic.

## Acceptance Criteria
- `npm run check:races` exits non-zero on any failure with a readable report; the Softie check is skipped with a notice when offline.
- PRD §12 assertions encoded (11 stations, Friday start, 38 h, first crew at 45.8, altitude flag, image-chart transcription flagged for human check).
- README "Races" section explains folders, generic mode, intake, re-intake, archive, and the check.

## Technical Context
- All prior beads; `scripts/race-intake.mjs`, `scripts/race-merge.mjs`, `scripts/race-config.mjs`.

## Constraints
- Must not require personal files (`*.private.json`) to pass.

## Out of Scope
CI setup on GitHub (local only).

## Related
Depends on every phase-3 bead and the results bead. PRD §12, §13.

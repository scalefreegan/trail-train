## What
Race-day live position + manual checkpoint. Pure helper web/src/race/checkpointHold.ts (+ scripts twin) `checkpointHold({station, clock|elapsed_h}, course, raceStart)` → the hold mile/elapsed used by the existing resolveHold path. Race-day page: when race.json.tracking exists, poll GET /api/races/:slug/tracker while open (60 s, backoff on failure), set the hold labelled "tracker · HH:MM" unless a manual hold is newer; a manual "passed <station> at HH:MM" form (station select + time) persisted per slug; AUTO clears both. Review screen edits tracking.bib/name.
## Acceptance Criteria
- node tests for checkpointHold; Playwright proof with a fixture tracker page served by a dev-only test route (TRAIL_TEST_FIXTURES=1) showing the hold updating; manual entry re-projects the next station; offline still works.
## Technical Context
web/src/race/RaceDay.tsx (resolveHold, raceDayHold.ts), useRaceData.ts, bead 05 endpoint, RaceIntake.tsx review fields.

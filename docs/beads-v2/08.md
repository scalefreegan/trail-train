## What
Crew export content. The crew page: header (race, date, start, goal), per-station table (best/expected/worst ETA at the goal, cutoff, crew/drop-bag flags, drive time + directions link text, notes), emergency contacts + crew rules, pickups per crew stop from the fuel plan, inline SVG map + elevation profile, the manual checkpoint updater ("passed <station> at HH:MM" → re-project locally using the embedded projection), a "last updated" stamp, print stylesheet (light, page breaks).
## Acceptance Criteria
- Playwright offline proof at 390 px and print (page.pdf light-pixel ≥ 95 %); updater changes downstream ETAs; no console errors; content checked against the live race view numbers for the same knobs.
## Technical Context
bead 07 shell, bead 06 checkpointHold, CrewSheet.tsx content as the reference, FuelCard/DropBagCard for pickups.

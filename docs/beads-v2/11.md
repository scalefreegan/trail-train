## What
Remaining UI flows + gate. Flows: print light-pixel check for the four cards + crew sheet; offline race-day (route failures) with a browsed archive not leaking; dialog a11y (role, focus trap, names) for every dialog; archive with a fixture activity; refresh diff using TRAIL_FAKE_AGENT; B-race quick form; crew export opens offline; widths 320/390/768/1024/1280 no horizontal scroll. `check:races` runs test:ui (skippable with `--no-ui` / TRAIL_CHECK_NO_UI=1) and reports its duration.
## Acceptance Criteria
- All flows green; check:races output shows the UI section; total check:races time reported.
## Technical Context
bead 10 harness; scripts/check-races.mjs.

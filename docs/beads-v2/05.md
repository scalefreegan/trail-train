## What
Tracker adapters. scripts/trackers/index.mjs registry (`detect(url)` → adapter, `fetchLastCheckpoint({url,bib,name}, fetchImpl)` → {station, clock, elapsed_h, source, at} | null) with adapters maprogress.mjs and opensplittime.mjs (UltraLive if time permits), each parsing a committed fixture page (HTML/JSON saved under scripts/fixtures/trackers/, scrubbed of personal data) and mapping checkpoint names to race.json aid stations with the existing aid-match name normaliser. race.json gains `tracking: {url, bib, name}` (intake fills url from links.tracking; validator optional). `GET /api/races/:slug/tracker` polls on demand with a 60 s cache TTL, cross-site guarded, never polls without a request; 404 when no adapter matches.
## Acceptance Criteria
- Tests per adapter on fixtures (last checkpoint, station mapping, no-runner-found → null, malformed page → error not crash); registry detection by hostname; endpoint TTL test with an injected fetch.
## Technical Context
scripts/aid-match.mjs normaliseName, scripts/race-config.mjs, web/vite.config.ts, docs/PRD-v2.md §4. The Softie site links MAProgress and OpenSplitTime.
